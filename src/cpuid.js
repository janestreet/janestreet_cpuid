// Provides: cpuid_maximum_value_and_brand
// Requires: caml_failwith
function cpuid_maximum_value_and_brand () {
    caml_failwith("CPUID instruction does not exist in Javascript.")
}

// Provides: cpuid_extended_feature_flags_subleaf0
// Requires: caml_failwith
function cpuid_extended_feature_flags_subleaf0 () {
    caml_failwith("CPUID instruction does not exist in Javascript.")
}

// Provides: intel_cpuid_version_and_feature_information
// Requires: caml_failwith
function intel_cpuid_version_and_feature_information () {
    caml_failwith("CPUID instruction does not exist in Javascript.")
}

// Provides: amd_cpuid_version_and_feature_information
// Requires: caml_failwith
function amd_cpuid_version_and_feature_information () {
    caml_failwith("CPUID instruction does not exist in Javascript.")
}
